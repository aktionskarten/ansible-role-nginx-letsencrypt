#!/bin/bash

python /opt/acme-tiny/acme_tiny.py --account-key {{key_dir}}/account.key --csr {{key_dir}}/{{letsencrypt_fqdn[0]}}.csr --acme-dir /var/www/challenges/ > /tmp/signed.crt || exit
cat /tmp/signed.crt {{key_dir}}/intermediate.pem > {{key_dir}}/{{letsencrypt_fqdn[0]}}.pem
